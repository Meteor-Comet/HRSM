<UserControl x:Class="HRSM.HRSMApp.Views.HouseListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000" 
             Background="#F0F5F9">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="5" Padding="10" Margin="0,0,0,10" BorderBrush="#DDD" BorderThickness="1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="房屋名称：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding SearchName, UpdateSourceTrigger=PropertyChanged}" Width="120" Height="26" Margin="0,0,15,0" VerticalContentAlignment="Center"/>

                <TextBlock Text="租赁类型：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox ItemsSource="{Binding RentTypes}" SelectedItem="{Binding SearchRentType}" Width="80" Height="26" Margin="0,0,15,0"/>

                <TextBlock Text="朝向：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox ItemsSource="{Binding Directions}" SelectedItem="{Binding SearchDirection}" Width="80" Height="26" Margin="0,0,15,0"/>

                <TextBlock Text="户型：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding SearchLayout, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="26" Margin="0,0,15,0" VerticalContentAlignment="Center"/>

                <Button Command="{Binding SearchCommand}" Content="查询" Width="80" Height="28" Background="#F0AD4E" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="3"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <ListBox Grid.Row="1" 
                 ItemsSource="{Binding HouseList}" 
                 Background="Transparent" 
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" IsItemsHost="True"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Width="320" Height="170" Background="White" CornerRadius="8" Margin="0,0,15,15" BorderBrush="#DDD" BorderThickness="1">
                        <Border.Effect>
                            <DropShadowEffect Color="#CCC" ShadowDepth="2" BlurRadius="5" Opacity="0.3"/>
                        </Border.Effect>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="35"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="25"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" CornerRadius="8,8,0,0">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#C5D9F1" Offset="0"/>
                                        <GradientStop Color="#8DB4E2" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <TextBlock Text="{Binding HouseName}" FontWeight="Bold" Foreground="#333" 
                                           VerticalAlignment="Center" Margin="10,0"/>
                            </Border>

                            <Grid Grid.Row="1" Margin="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" CornerRadius="4" BorderBrush="#EEE" BorderThickness="1" Background="#F9F9F9">
                                    <Image Source="{Binding HousePic}" Stretch="Uniform"/>
                                </Border>

                                <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Margin="0,0,0,5" FontSize="12">
                                        <Run Text="业主：" Foreground="#666"/>
                                        <Run Text="{Binding OwnerName}" Foreground="#D9534F" FontWeight="Bold"/>
                                    </TextBlock>

                                    <TextBlock Margin="0,0,0,5" FontSize="12" Foreground="#666">
                                        <Run Text="朝向："/>
                                        <Run Text="{Binding HouseDirection}"/>
                                        <Run Text="   "/>
                                        <Run Text="租售："/>
                                        <Run Text="{Binding RentType}"/>
                                    </TextBlock>

                                    <TextBlock Margin="0,0,0,5" FontSize="12" Foreground="#666">
                                        <Run Text="户型："/>
                                        <Run Text="{Binding HouseLayout}"/>
                                    </TextBlock>

                                    <TextBlock FontSize="12">
                                        <Run Text="状态：" Foreground="#666"/>
                                        <Run Text="{Binding HouseState}" Foreground="Green" FontWeight="Bold"/>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>

                            <Button Grid.Row="2" Content="详情" 
                                HorizontalAlignment="Right" Margin="0,0,15,5" 
                                Foreground="#007ACC" Cursor="Hand" FontWeight="Bold" FontSize="12"
                                Background="Transparent" BorderThickness="0"
                                Command="{Binding DataContext.ShowDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>